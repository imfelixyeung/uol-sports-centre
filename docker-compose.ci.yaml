version: "3"

services:
  auth:
    build:
      context: ./services/auth
      target: ci

  status:
    build:
      context: ./services/status
      target: ci

  users:
    build:
      context: ./services/users
      target: ci

  docs:
    build:
      context: ./docs
      target: ci
